<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Production: all traffic MUST use HTTPS with certificate pinning.
        Prevents MITM attacks even if a CA is compromised.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Certificate pinning for Magneetar API -->
    <domain-config>
        <domain includeSubdomains="true">api.magneetar.me</domain>
        <pin-set expiration="2027-01-01">
            <!-- Cloudflare edge certificate (SHA-256) -->
            <pin digest="SHA-256">Xq8fpdR3BhJK5hvia4K1+Uy32Xxp6sIEo99DLNPMJBk=</pin>
            <!-- Backup: Let's Encrypt intermediate CA -->
            <pin digest="SHA-256">CUXRMVEPCyTh37t/I/AQFw3XzMaKw+BL/4MrFQ5c=</pin>
        </pin-set>
    </domain-config>

    <!-- Dev/self-host exceptions -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
